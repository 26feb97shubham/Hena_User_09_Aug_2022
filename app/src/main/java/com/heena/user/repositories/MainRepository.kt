package com.heena.user.repositories

import com.heena.user.`interface`.APIInterface
import retrofit2.http.Body

class MainRepository constructor(private val apiInterface: APIInterface) {
//    fun login() = apiInterface.login()
}